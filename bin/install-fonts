#!/bin/bash -e
# This script should download and install fonts into `.fonts` directory

curl --location https://github.com/cadsondemak/Sarabun/archive/master.tar.gz | tar xvz -C .fonts --strip-components=2 --wildcards '*.ttf'
